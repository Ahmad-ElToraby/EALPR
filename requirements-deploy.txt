# ============================================
# EALPR Cloud Deployment Dependencies
# CPU-only — no GPU drivers needed in cloud
# ============================================

# Core ML
ultralytics>=8.0.0
paddlepaddle>=2.6.0
paddleocr>=2.6.0

# Web Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Image Processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0

# NOTE: torch and torchvision are installed separately
# in the Dockerfile using CPU-only index to save ~1.5GB
